/*Viables defined in the test suite:
- oldID
- currentAge

You should NOT try to redefine any of the above variables.
*/

// Write your code below this line!
let newID = 1000000000 + oldID;

const ageIsValid = if (currentAge == integer) { true

} else { false

}
const randomNumber = math.floor(math.randomNumber()*20 + 0);

const randomUserID = randomNumber + 1000000000;
